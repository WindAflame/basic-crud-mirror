# TODO

## back_express_swagger_jsdoc

- [ ] Add CORS middleware
- [ ] `POST /books/create_book` — return 201 instead of 200
- [ ] `DELETE /books/delete_book/:book_title` — add path parameter
- [ ] Makefile — add `make install` target
- [ ] Makefile — add `make dev` target

## back_express_swagger_tsoa

- [ ] Add CORS middleware
- [ ] `POST /books/create_book` — return 201 instead of 200
- [ ] `DELETE /books/delete_book/:book_title` — add path parameter
- [ ] Makefile — add `make install` target

## back_fastapi

- [ ] Add CORS middleware
- [ ] `POST /books/create_book` — return 201 instead of 200
- [ ] `PUT /books/update_book` — explicitly return 404 when book not found

## back_flask

- [ ] Add CORS middleware
- [ ] `POST /books/create_book` — return 201 instead of 200
- [ ] `PUT /books/update_book` — return 404 when book not found instead of always 200
- [ ] Makefile — add `make dev` target

## back_flask_2

- [ ] Add CORS middleware
- [ ] `POST /books/create_book` — return 201 instead of 200
- [ ] `PUT /books/update_book` — fix incomplete logic

## back_nestjs_swagger

- [ ] Implement the project (currently only a git stub)
- [ ] Add CORS middleware
- [ ] Implement all routes per [docs/02-resources.md](../docs/02-resources.md)
- [ ] Add Makefile with `make install` and `make dev`
- [ ] Add `Dockerfile` or `.devcontainer`
